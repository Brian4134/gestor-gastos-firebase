/**
 * Exporta la instancia de Firestore desde la configuración de Firebase
 * Este archivo mantiene compatibilidad con las importaciones existentes
 */
import { db } from "../config/firebase.js";

export default db;
